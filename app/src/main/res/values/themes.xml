<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EngZi" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <!--        <item name="colorOnPrimary">@color/white</item>-->
        <item name="android:textColor">@color/primary</item>
        <item name="android:colorBackground">@color/background_primary</item>
        <item name="colorAccent">?android:attr/textColor</item>
        <item name="android:fontFamily">@font/bevietnampro_medium</item>

        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
  <!-- Base application theme. -->
    <style name="Base.Base.Theme.EngZi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>